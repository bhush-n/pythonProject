a1 = {3, 5, 6, 3, 4, 8}
a2 = {3, 5, 6, 3, 10, 11, 12, 16}

# a1.clear()  # It will clear the set
# a1.pop()  # It will remove the random element from set
# a1.add(9)  # It will add an element in set
# print(a1.union(a2))  # It will merge the a2 set into a1 and return a new set
# print(a1.intersection(a2))  # It will return the common elements between two sets with a new set

print(a1)
print(a2)
