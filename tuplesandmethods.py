t = (1, 2, 3, 4)

print(t.count(2))  # It will count the elements
print(t.index(2))  # It will print the index no. of an element

t[0] = 56  # It will throw an error because tuples are immutable and cannot be changed
